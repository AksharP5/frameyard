VITE_LOCAL_MODE=true

# Frameyard runs without a hosted account. Set VITE_LOCAL_MODE=false only if
# you operate compatible services of your own. Do not use upstream credentials
# or endpoints in a public build.
